{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Neptune: Electron (open test.todo)",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "args": ["${workspaceFolder}/src/index.js", "${workspaceFolder}/test.todo"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
